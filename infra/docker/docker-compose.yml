services:
  db:
    image: postgres
    container_name: aurora-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres_password
      POSTGRES_DB: aurora_comercial
    ports:
      - "5432:5432"
    volumes:
      - ../../db/init:/docker-entrypoint-initdb.d
    restart: unless-stopped
